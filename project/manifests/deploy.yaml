apiVersion: apps/v1
kind: Deployment
metadata:
  name: project
  namespace: dwk-mooc
  labels:
    app: project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: project
  template:
    metadata:
      labels:
        app: project
    spec:
      containers:
        - name: log-output
          image: ghcr.io/fazstrac/dwk-project:0de0ab84310fb92d7739e19471726c77e78b111c
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault